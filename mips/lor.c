logical_or(short a, short b) {
  return a || b; // should return true or false, like 1 or 0.
}

bitwise_or(char c, char d) {
  return c | d; // should do like 'A' lor 'B' = 1010 or 1011 = 00...001011
}
